# Task Tracker
 This application is a simple task tracker that allows a user to create, update, retrieve, delete and track the progress of a task

 ## Running it locally
To run this project on your local machine, clone the repository using the command below, 

````
git clone 
````

Enter the project useing
```
cd  tasks
```
Start the development server using 
